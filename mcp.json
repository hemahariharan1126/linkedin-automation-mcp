{
  "mcpServers": {
    "linkedin-automation": {
      "command": "node",
      "args": ["src/index.mjs"],
      "env": {
        "LINKEDIN_ACCESS_TOKEN": "${LINKEDIN_ACCESS_TOKEN}"
      }
    }
  }
}